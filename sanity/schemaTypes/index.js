import about from "./about";
import landingPage from "./landingPage";
import locations from "./locations";
import projects from "./projects";

export const schema = {
  types: [locations, about, projects, landingPage],
};
